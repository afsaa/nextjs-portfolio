import React from 'react';

const ErrorContainer = () => {
  return <div className="flex items-center justify-center">An error occurred while fetching the data.</div>;
};

export default ErrorContainer;
